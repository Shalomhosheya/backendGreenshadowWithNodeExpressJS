export class Field {
    fieldID!: string;
    fieldName!: string;
    fieldLocation!: string;
    fieldSize!: string;
    staff!: string;
    pic1!: Buffer; // Corrected
    pic2!: Buffer; // Corrected
}
